import React from "react"
import ReactDOM from "react-dom/client"
import BeautimaxHome from "../BeautimaxHome"

ReactDOM.createRoot(document.getElementById("root")!).render(
    <React.StrictMode>
        <BeautimaxHome showNav />
    </React.StrictMode>
)
